# 🤖 Qwen Chat UI — ממשק צ'אט מקומי

ממשק משתמש מקצועי לצ'אט עם מודל Qwen 2.5 שרץ מקומית על המחשב שלך.  
תומך עברית ואנגלית, כולל שליטה מלאה בפרמטרים והגנת משקולות.

---

## 📁 מבנה הפרויקט

```
qwen-chat-project/
│
├── app.py                  ← שרת Flask + חיבור למודל + API הגנת משקולות
├── simple_chat.py          ← צ'אט פשוט בטרמינל (הגרסה המקורית)
├── test_setup.py           ← בדיקת התקנת PyTorch
│
├── templates/
│   └── index.html          ← ממשק המשתמש (UI)
│
├── requirements.txt        ← רשימת חבילות נדרשות
├── .gitignore
│
├── install_and_run.bat     ← התקנה + הפעלה (Windows) ⭐
├── run.bat                 ← הפעלה בלבד (Windows)
├── install_and_run.sh      ← התקנה + הפעלה (Linux/Mac)
│
└── README.md               ← הקובץ הזה
```

---

## 🚀 התקנה והפעלה — Windows

### דרך מהירה (מומלץ!)

1. **חלץ את קובץ ה-ZIP** לתיקייה לבחירתך
2. **לחץ דאבל-קליק** על `install_and_run.bat`
3. **המתן** — הסקריפט יתקין הכל ויפעיל את השרת
4. **פתח בדפדפן:** `http://localhost:5000`

> 💡 בהפעלות הבאות, השתמש ב-`run.bat` (מהיר יותר, דילוג על התקנה)

### דרך ידנית

```cmd
:: פתח CMD בתיקיית הפרויקט

:: שלב 1: צור סביבה וירטואלית
python -m venv .venv

:: שלב 2: הפעל את הסביבה
.venv\Scripts\activate

:: שלב 3: התקן חבילות
pip install -r requirements.txt

:: שלב 4: הפעל את השרת
python app.py

:: שלב 5: פתח בדפדפן
:: http://localhost:5000
```

---

## 🐧 התקנה והפעלה — Linux / Mac

### דרך מהירה

```bash
# חלץ ותן הרשאות
chmod +x install_and_run.sh

# הפעל
./install_and_run.sh
```

### דרך ידנית

```bash
# צור סביבה
python3 -m venv .venv
source .venv/bin/activate

# התקן
pip install -r requirements.txt

# הפעל
python app.py

# פתח: http://localhost:5000
```

---

## 🎯 תכונות

### 💬 צ'אט
- ממשק צ'אט מלא עם היסטוריית שיחה
- **שני מודלים**: Qwen 2.5 0.5B ו-1.5B — החלפה חיה מה-UI
- תמיכה בעברית ואנגלית עם מתג שפה
- תבניות שאילתות מורכבות (ניתוח, השוואה, קוד, סיכום)
- מדידת ביצועים לכל תשובה עם שם המודל

### ⚙️ שליטה בפרמטרים
| פרמטר | תיאור | טווח |
|--------|--------|------|
| Temperature | רמת יצירתיות | 0.05 – 2.0 |
| Top-P | סף הסתברות מצטברת | 0.1 – 1.0 |
| Top-K | מספר טוקנים לדגימה | 0 – 200 |
| Repetition Penalty | עונש על חזרות | 1.0 – 2.0 |
| No Repeat N-gram | מניעת חזרה על רצפים | 0 – 10 |
| Max Tokens | אורך תשובה מקסימלי | 32 – 2048 |
| Num Beams | Beam Search | 1 – 8 |
| Length Penalty | עונש/בונוס לאורך | 0.5 – 2.0 |
| Do Sample | דגימה/Greedy | On/Off |
| Early Stopping | עצירה מוקדמת | On/Off |

**4 הגדרות מהירות (Presets):** מדויק, מאוזן, יצירתי, Beam Search

### 🛡️ הגנת משקולות
- **נעילה/פתיחה** של כל המשקולות בלחיצה אחת
- **שליטה לפי שכבה** — נעילה/פתיחה סלקטיבית
- **איפוס למקור** — החזרת כל המשקולות לערכים המקוריים
- **Diff Viewer** — צפייה בשינויים מהמקור עם ערך Δ לכל שכבה
- **ניסוי רעש** — הוספת רעש מבוקר לבדיקות
- **היסטוריית פעולות** — לוג מלא

### 📊 דשבורד סטטוס
- פרטי מודל (שם, פרמטרים, התקן, דיוק)
- תצורה פעילה — כל הפרמטרים הנוכחיים
- ביצועי תגובה — זמן, טוקנים/שנייה, קלט/פלט

---

## ⚠️ דרישות מערכת

- **Python** 3.10 ומעלה
- **RAM** מינימום 4GB למודל 0.5B, מומלץ 8GB+ למודל 1.5B
- **דיסק** ~5GB (שני מודלים + חבילות)
- **אינטרנט** נדרש להורדה ראשונה בלבד

> ⏱ ההורדה הראשונה של המודל לוקחת 1-5 דקות (תלוי בחיבור).  
> לאחר מכן המודל נשמר מקומית ב-cache ואין צורך בחיבור.

---

## 🔧 פתרון בעיות

| בעיה | פתרון |
|------|-------|
| `python is not recognized` | התקן Python והוסף ל-PATH |
| `ModuleNotFoundError` | הפעל `pip install -r requirements.txt` מתוך הסביבה |
| השרת לא עולה | בדוק שפורט 5000 פנוי, או שנה בסוף `app.py` |
| מודל לא יורד | בדוק חיבור אינטרנט, נסה שוב |
| תגובות איטיות | נורמלי ל-CPU. תגובות של 5-30 שניות צפויות |

---

## 📜 קבצי הפרויקט

| קובץ | תפקיד |
|------|--------|
| `app.py` | שרת Flask, חיבור למודל, API לצ'אט + הגנת משקולות |
| `templates/index.html` | ממשק UI מלא (HTML/CSS/JS) |
| `simple_chat.py` | צ'אט פשוט בטרמינל (ללא UI) |
| `test_setup.py` | בדיקת התקנת PyTorch |
| `requirements.txt` | רשימת חבילות Python |

---

## English Quick Start

```bash
# 1. Extract ZIP
# 2. Open terminal in project folder

# Windows:
install_and_run.bat

# Linux/Mac:
chmod +x install_and_run.sh
./install_and_run.sh

# 3. Open http://localhost:5000
```

Features: Full chat UI, **dual model** (0.5B & 1.5B with live switching), bilingual (Hebrew/English), all generation parameters control, weight protection system with freeze/unfreeze/reset/per-layer control, performance dashboard, query templates.
